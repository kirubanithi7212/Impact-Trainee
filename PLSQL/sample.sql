use practice;
create table products (productId int,productName varchar(20),quantity int(5),price int(5));
select * from products;
insert into products values(4,'power bank',5,8500);

/*this procedure is for showing product details*/
